---
import BaseLayout from "@layouts/BaseLayout.astro";

const title = "Contact — Sabbahi Sonia";
const description = "Me contacter pour une mission, un projet ou un échange professionnel.";
---

<BaseLayout {title} {description}>
  <section class="py-16 md:py-24 max-w-4xl mx-auto px-4 space-y-12">

    <!-- TITRE -->
    <div class="space-y-3 text-center">
      <h1 class="text-4xl font-bold">Me contacter</h1>
      <p class="text-lg opacity-80 max-w-2xl mx-auto">
        Pour une mission freelance, un projet ou simplement échanger autour du développement,
        vous pouvez me contacter via le formulaire ou directement par email.
      </p>
    </div>

    <!-- DEUX COLONNES -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-10 items-start">

      <!-- INFOS CONTACT -->
      <div class="space-y-4 text-sm">
        <h2 class="text-xl font-semibold">Coordonnées</h2>
        <p class="opacity-80">
          <span class="font-medium">Email :</span><br />
          <a href="mailto:sonia.sabbahi.dev@gmail.com" class="text-vermillion hover:underline">
            sonia.sabbahi.dev@gmail.com
          </a>
        </p>

        <p class="opacity-80">
          N’hésitez pas à détailler votre besoin : type de projet, délai, budget approximatif,
          contraintes techniques éventuelles.
        </p>
      </div>

      <!-- FORMULAIRE (Netlify Forms) -->
      <div>
        <form
          name="contact"
          method="POST"
          data-netlify="true"
          netlify-honeypot="bot-field"
          action="/merci"
          class="space-y-4 rounded-xl border border-neutral-300/60 dark:border-neutral-700/60 bg-white/70 dark:bg-neutral-900/70 px-4 py-5 shadow-sm backdrop-blur"
        >
          <!-- Champ caché obligatoire pour Netlify -->
          <input type="hidden" name="form-name" value="contact" />

          <!-- Champ honeypot (anti-spam) -->
          <p class="hidden">
            <label>
              Ne pas remplir ce champ :
              <input name="bot-field" />
            </label>
          </p>

          <div class="space-y-1">
            <label for="name" class="text-sm font-medium">Nom / Prénom</label>
            <input
              id="name"
              name="name"
              type="text"
              required
              class="w-full rounded-lg border border-neutral-300/70 dark:border-neutral-700/70 bg-white dark:bg-neutral-900 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-vermillion focus:border-vermillion"
              placeholder="Votre nom"
            />
          </div>

          <div class="space-y-1">
            <label for="email" class="text-sm font-medium">Email</label>
            <input
              id="email"
              name="email"
              type="email"
              required
              class="w-full rounded-lg border border-neutral-300/70 dark:border-neutral-700/70 bg-white dark:bg-neutral-900 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-vermillion focus:border-vermillion"
              placeholder="vous@exemple.com"
            />
          </div>

          <div class="space-y-1">
            <label for="subject" class="text-sm font-medium">Sujet (optionnel)</label>
            <input
              id="subject"
              name="subject"
              type="text"
              class="w-full rounded-lg border border-neutral-300/70 dark:border-neutral-700/70 bg-white dark:bg-neutral-900 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-vermillion focus:border-vermillion"
              placeholder="Création de site, application, refonte..."
            />
          </div>

          <div class="space-y-1">
            <label for="budget" class="text-sm font-medium">Budget / fourchette (optionnel)</label>
            <input
              id="budget"
              name="budget"
              type="text"
              class="w-full rounded-lg border border-neutral-300/70 dark:border-neutral-700/70 bg-white dark:bg-neutral-900 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-vermillion focus:border-vermillion"
              placeholder="Ex : 800–1500 €"
            />
            <p class="text-[11px] opacity-60">
              Cela m’aide à vous proposer une solution réaliste et adaptée.
            </p>
          </div>

          <div class="space-y-1">
            <label for="message" class="text-sm font-medium">Message</label>
            <textarea
              id="message"
              name="message"
              rows="4"
              required
              class="w-full rounded-lg border border-neutral-300/70 dark:border-neutral-700/70 bg-white dark:bg-neutral-900 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-vermillion focus:border-vermillion"
              placeholder="Parlez-moi de votre projet, de vos objectifs et de vos délais..."
            ></textarea>
          </div>

          <button
            type="submit"
            class="mt-2 w-full md:w-auto px-6 py-2 rounded-lg bg-vermillion text-white text-sm font-medium hover:bg-rose-600 transition-transform duration-200 hover:-translate-y-0.5 focus:outline-none focus:ring-2 focus:ring-vermillion focus:ring-offset-2 focus:ring-offset-white dark:focus:ring-offset-neutral-900"
          >
            Envoyer le message
          </button>

          <p class="text-xs opacity-60 mt-2">
            Si le formulaire présente un problème, vous pouvez aussi m’écrire directement à
            <span class="font-medium"> sonia.sabbahi.dev@gmail.com</span>.
          </p>
        </form>
      </div>

    </div>

  </section>
</BaseLayout>



